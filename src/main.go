package main

func main() {
	var x int = 153
	var a int = x / 100
	var b int = (x/10)%10
	var c int = x % 10
	var d = a*a*a = b*b*b + c*c*c
	switch d {
	case x:
		fmt.Println(x)
	default:
		fmt.Println(x)
	}
}
